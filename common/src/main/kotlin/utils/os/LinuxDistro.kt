package utils.os

// TODO: Complete this later, might use $DESKTOP_SESSION to detect it
enum class LinuxDistro {
    Fedora,
    Ubuntu,
    Debian,
    Manjaro,
    ArchLinux,
    PopOs,
    LinuxMint,
    CentOS,
    OpenSUSE,
    ElementaryOS,
    ZorinOS,
}
